{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "cd frontend && npm install --legacy-peer-deps --loglevel=error --no-audit --no-fund && npm run build",
  "outputDirectory": "frontend/.next"
}
